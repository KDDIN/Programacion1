program practico3_ejercicio1;

var

    booleano: boolean;
    
begin
    { 0 < 1, 'A' < 'Z', TRUE < FALSE, ('b' < 'd') AND (0 < 1),
    (5 < 1) OR (0 > -1)}
    booleano := 0 < 1;
    writeln(booleano);

end.